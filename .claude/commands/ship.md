以下の手順を順番に実行してください。

## 1. 変更内容を確認

以下のコマンドを並列で実行して変更内容を把握してください：
- `git status`
- `git diff HEAD`
- `git log --oneline -5`

## 2. コミットメッセージを自動生成

差分を分析して、以下のルールで日本語のコミットメッセージを作成してください：
- 1行目：変更の要約（50文字以内）
- 必要に応じて改行後に詳細を追記
- 末尾に必ず以下を付与：
  ```
  Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
  ```

## 3. ステージングしてコミット

```bash
git add -A
git commit -m "<生成したコミットメッセージ>"
```

## 4. プッシュ

現在のブランチにプッシュしてください：
```bash
git push origin <現在のブランチ名>
```

各ステップの完了をユーザーに報告しながら進めてください。
